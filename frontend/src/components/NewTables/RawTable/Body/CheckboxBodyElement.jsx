import React, { Component } from 'react';

const propTypes = {
};

const defaultProps = {
};

export default function CheckboxBodyElement({ }) {
  return (<h1>Hi</h1>);
}

CheckboxBodyElement.propTypes = propTypes;
CheckboxBodyElement.defaultProps = defaultProps;